<form method="post" action="{{ path('app_cotation_delete', {'id': cotation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotation.id) }}">
    <button class="btn">Delete</button>
</form>
