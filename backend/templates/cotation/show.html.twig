{% extends 'base.html.twig' %}

{% block title %}Cotation{% endblock %}

{% block body %}
    <h1>Cotation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotation.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ cotation.value }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ cotation.date ? cotation.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cotation_index') }}">back to list</a>

    <a href="{{ path('app_cotation_edit', {'id': cotation.id}) }}">edit</a>

    {{ include('cotation/_delete_form.html.twig') }}
{% endblock %}
